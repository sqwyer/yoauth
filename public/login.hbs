<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="{{path}}/static/style.css">
    <title>Login</title>

    <style>
        body {
            background: {{background}};
        }

        .continue {
            background: {{button_background}} !important;
        }
        /
    </style>
</head>
<body>
    <div class="flex-container">
        <div class="box">
            <div class="selections">
                <a class="selector signup" href="{{path}}/signup">Sign up</a>
                <a class="selector login active" href="#">Login</a>
            </div>
            <div class="main">
                {{!-- <a class="btn" href="#">
                    <img src="{{path}}/static/google.svg"/>
                    <span>Login with Google</span>
                </a>
                <a class="btn" href="#">
                    <img src="{{path}}/static/apple.svg"/>
                    <span>Login with Apple</span>
                </a> --}}
                <form action="{{path}}/login" method="POST">
                    {{#if errorMessage}}
                        <p class="err">{{errorMessage}}</p>
                    {{/if}}
                    <label>Email</label>
                    <input name="email" type="email" placeholder="johndoe@gmail.com"/>
                    <label>Password</label>
                    <input name="password" type="password" placeholder="••••••••••">
                    <a class="btn continue" onclick="document.getElementsByTagName('FORM')[0].submit();">
                        Continue
                    </a>
                </form>
            </div>
        </div>
    </div>
</body>
</html>